import type { RequestHandler } from "@sveltejs/kit";

export const GET: RequestHandler = ({ url }) => {
  const response = new Response('loaderio-63cba677f74a873cfe7c8396b3eb8388');

  return response;
}
